# XTools 代码审查修复任务清单

> 审查日期：2026-03-20
> 修复完成：2026-03-20
> 审查范围：全部 21 个模块，557 个源文件

## P0 - 严重问题（必须修复）

- [x] **S-1** BlueprintExtensions: FKCHandler MapTerm 空指针解引用（6个文件）
- [x] **S-2** EnhancedCodeFlow: 后台线程调用 `TWeakObjectPtr::Get()` 非线程安全
- [x] **S-3** EnhancedCodeFlow: ECFWaitUntil 协程双重 resume
- [x] **S-4** ObjectPool: Preallocator 创建的 Actor 不注册到池列表
- [x] **S-5** ObjectPool: `ClearPool` 是空操作但返回 true
- [x] **S-6** SortEditor: 静态 `bIsRebuilding` 跨实例共享
- [x] **S-7** Build.cs: XToolsCore 的 uplugin PlatformAllowList 缺少 Android/iOS

## P1 - 中等问题（应该修复）

- [x] **M-1** ObjectPool: `AcquireDeferred` 新建路径不注册 ActiveActors/AllActorsSet（由 S-4 一并修复）
- [x] **M-2** ObjectPool: `FinalizeDeferred` 新建路径只加 ActiveActors 不加 AllActorsSet（由 S-4 一并修复）
- [x] **M-3** BlueprintExtensions: `ConditionalSequence`/`MultiConditionalSelect` 移除 `Super::ExpandNode`
- [x] **M-4** BlueprintExtensions: `MapAppend`/`MapIdentical` 保留已序列化类型
- [x] **M-5** BlueprintExtensions: 消除双重类型传播回调（6个文件）
- [x] **M-6** EnhancedCodeFlow: Timeline 循环模式 while 改用 `FMath::Fmod`
- [x] **M-7** EnhancedCodeFlow: Timeline 结束帧 TickFunc 不再重复调用
- [x] **M-8** EnhancedCodeFlow: 两个 `Delay` 重载输入验证统一
- [x] **M-9** EnhancedCodeFlow: `FECFInstanceId::DynamicIdCounter` 改为 `std::atomic`
- [x] **M-10** XToolsCore: ErrorReporter 支持传入调用点 `__FILE__`/`__LINE__`，新增便捷宏
- [x] **M-11** XToolsCore: 4个防御性宏添加 `do-while(0)` 包装
- [x] **M-12** PointSampling: 运行时纹理采样添加像素越界检查
- [x] **M-13** PointSampling: ARM 平台改用 `FMemory::Memcpy` 安全读取
- [x] **M-14** FieldSystemExtensions: `CachedGeometryCollections` 改为 `TWeakObjectPtr`
- [x] **M-15** FormationSystem: `DoPathsIntersect` 的 `Threshold` 参数已应用
- [x] **M-16** X_AssetEditor: CollisionManager 4个批量操作添加 `FScopedTransaction`
- [x] **M-17** ScreenshotTool: `CachedGraphEditorsForWarmup` 改为 `TWeakPtr`
- [x] **M-18** Build.cs: `EnhancedCodeFlow` Core/CoreUObject/Engine 移至 PublicDependency
- [x] **M-19** Build.cs: `ComponentTimelineRuntime` 和 `FormationSystem` 添加 XToolsCore 依赖
- [x] **M-20** Build.cs: `ComponentTimelineUncooked` uplugin 添加 AdditionalDependencies

## P2 - 低等问题（建议改进）

- [x] **L-1** XToolsCore: 6 个未使用防御性宏标注为预留 API
- [x] **L-2** XToolsCore: 版本号更新为 v1.9.6（XToolsDefines.h + uplugin）
- [x] **L-3** XToolsCore: 日志分类列表同步（修正2个错误名称，补充20个缺失分类）
- [x] **L-4** ObjectPool: 统计字段已通过 PoolLock 保护，添加注释说明
- [x] **L-5** BlueprintExtensions: Map 操作节点添加 TODO（提取基类减少重复）
- [x] **L-6** EnhancedCodeFlow: 删除 6 个未使用常量，保留 MaxDelayTime
- [x] **L-7** Sort: 去重函数已确认为最优实现，添加注释
- [x] **L-8** PointSampling: BulkData RAII 封装添加 TODO
- [x] **L-9** FormationSystem: Boids O(N^2) 空间分区优化添加 TODO
- [x] **L-10** Build.cs: 移除 `X_AssetEditor` 冗余 `WITH_EDITOR=1` 定义
