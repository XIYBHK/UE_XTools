# 待发布更新 (UNRELEASED)

> 此文档记录尚未发布的功能更新、修复和移除，用于下次版本发布时的描述参考。
> 发布新版本时，将此文件内容合并到 CHANGELOG.md，然后清空此文件（保留说明部分）。

---

### CI/CD
- 调整 Actions 至 Node 24

### XTools_AutoSizeComments
- 修复 多图表面板刷新请求互相覆盖
- 保留 上游刷新队列并兼容本地生命周期清理
- 修复 通知按钮回调越过模块生命周期
- 优化 模块关闭时主动清理通知与委托

### XTools_BlueprintScreenshotTool
- 修复 缩放后截图尺寸越界
- 测试 增加截图尺寸上下界护栏

### XTools_BlueprintAssist
- 审查 同步上游功能并保留本地配置扩展
- 修复 图表命令关闭时未注销
- 修复 全局图表菜单扩展未注销

### XTools_ElectronicNodes
- 优化 切片线交叉连线延迟绘制与淡化
- 兼容 UE 5.6+ 向量类型及 PCG 黑线显示

### AxisLocker
- 修复 状态恢复误用当前目标
- 修复 关闭模式误报遗留轴锁
- 测试 增加关闭模式查询护栏

### BlueprintExtensionsRuntime
- 修复 Map空结果重复初始化输出
- 修复 切换样条复用旧距离缓存
- 修复 字符串反射接受尾随垃圾
- 优化 反射写入失败保持原值
- 调整 变量反射开放C++调用
- 调整 对象扩展开放C++调用
- 测试 增加反射写入原子性护栏
- 测试 增加对象C++调用护栏

### BlueprintExtensions
- 修复 Map查找漏断不兼容连接
- 修复 孤立多分支、多条件选择与 Map 节点触发 Checked 蓝图查询
- 修复 Case 节点编辑时解引用缺失蓝图或图表
- 修复 Map 合并未拒绝类型不一致的输入
- 修复 Map 相同判断误接受未解析的 Wildcard 类型
- 修复 K2展开故障仅产生编译警告
- 修复 容器循环中间引脚失败触发断言
- 修复 容器循环无效输入仅产生警告
- 修复 Map元素节点无效终端仅产生警告
- 修复 孤立Map元素节点重建解引用空图表
- 修复 控制流节点展开缺少完整校验
- 修复 赋值与Map查找错误降级警告
- 修复 动态分支缺半对触发断言
- 修复 延迟循环缺失引脚触发断言
- 修复 Map与Set循环残缺节点断言
- 测试 增加孤立节点编辑与重建安全护栏
- 测试 扩展六类Map元素节点孤立重建护栏
- 测试 增加 Map 合并与相同判断真实展开编译护栏
- 测试 增加三类容器循环真实编译护栏
- 测试 增加容器输入错误级别护栏
- 测试 收紧缺失必需引脚为编译错误
- 测试 增加控制流与Map查找编译护栏
- 测试 增加不完整分支配对护栏
- 测试 覆盖五类延迟循环残缺节点
- 测试 增加Map与Set循环残缺节点护栏
- 调整 循环与随机洗牌测试显式声明预期日志

### XTools_EnhancedCodeFlow
- 兼容 补齐配置缓存头文件显式包含
- 新增 六类时间轴支持正向/反向播放及运行中方向切换
- 新增 蓝图提供设置方向与反转动作控制
- 新增 六类时间轴支持命名事件轨道与正反向回调
- 优化 循环反向播放按原生时间轴边界处理
- 优化 事件轨道使用稳定排序、二分范围查询与每动作分段诊断
- 修复 曲线时间轴结束帧事件晚于完成回调导致蓝图事件丢失
- 修复 曲线时间轴自然结束帧重复输出终点 Tick
- 修复 循环精确端点重复派发及回调重入后继续完成
- 测试 增加时间轴方向、循环、事件顺序与重入行为自动化护栏

### SplineMovement
- 修复 前瞻目标越过端点时提前广播成功
- 修复 非有限前瞻、偏移或输入权重污染异步移动状态
- 测试 增加正反向实际到达端点护栏
- 测试 增加非有限移动参数拒绝护栏

### QueueSpline
- 修复 组件重新 BeginPlay 后拒绝成员注册
- 修复 移动事件回调注销成员后仍广播陈旧目标
- 测试 增加 EndPlay 后恢复生命周期护栏
- 测试 增加移动事件重入注销护栏并清理预期告警

### XTools_ComponentTimeline
- 修复 蓝图差异视图遗漏线性颜色轨道
- 修复 动态时间轴未加入 Actor 实例组件集合
- 修复 普通 UObject 时间轴所有者无法取得宿主 World
- 修复 孤立或不完整时间轴节点触发编辑器硬断言
- 修复 时间轴差异比较解引用无效节点或模板
- 测试 增加动态时间轴组件归属与注册护栏
- 测试 增加孤立组件时间轴节点安全降级护栏

### FieldSystemExtensions
- 修复 运行时关闭 Spawn 监听配置后 EndPlay 未注销既有监听器
- 修复 EndPlay 后保留 GC、筛选器及应用状态缓存
- 修复 刷新筛选缓存后旧 GC 残留 InitializationFields 引用
- 修复 关闭筛选后重新应用仍保留旧筛选器缓存
- 优化 Field System 蓝图用户警告接入统一错误报告
- 测试 增加 Field Actor 生命周期清理护栏
- 测试 增加筛选刷新解除旧 GC 注册护栏

### FormationSystem
- 修复 对齐力输入数组长度不一致时仍继续索引导致的越界风险
- 修复 非有限移动参数污染组件状态与移动输入
- 测试 增加不匹配位置/速度数组的防御性护栏
- 测试 增加非法重定向保持既有移动状态护栏

### GeometryTool
- 修复 非有限采样参数污染输出
- 修复 极端形状尺寸在采样计数转换前溢出
- 优化 几何采样用户警告接入统一错误报告
- 测试 扩展矩形、圆环与随机变换的非有限输入护栏

### PointSampling
- 修复 G8 源纹理末尾像素被误按四字节边界拒绝采样
- 修复 缓存键默认构造未初始化
- 修复 半周等价旋转重复缓存
- 修复 泊松入口接受非有限参数
- 测试 增加 G8 最后像素读取护栏
- 测试 增加等价旋转缓存命中护栏
- 测试 增加泊松输入验证护栏

### RandomShuffles
- 修复 权重数组多于输入数组时违背蓝图等长契约而被静默接受
- 修复 严格权重求和溢出偏置
- 测试 覆盖普通加权与严格权重采样的多余权重拒绝路径
- 测试 增加极值权重分配护栏

### 构建
- 验证 UE 5.8 StrictIncludes 三目标打包通过

## 日志格式说明

### 模块分类
按模块名称分为独立章节，每个章节内使用简洁的列表格式记录变更

### 记录格式
```markdown
### 模块名
- 类型 描述
- 类型 描述
```

### 类型说明（单字或双字）:
- **新增** / 新增功能
- **优化** / 改进现有功能
- **修复** / 修复问题
- **移除** / 删除功能
- **调整** / 参数或行为调整
- **集成** / 新增模块集成
- **本地化** / 本地化工作

### 内容要求
1. **简洁**：每行记录一个独立变更，描述控制在 20 字内
2. **清晰**：使用"动词 + 对象 + （效果）"格式
3. **关键信息**：包含重要的参数变化、性能数据、修复的问题
4. **不要**：避免详细技术细节、文件路径、长篇问题分析

### 发布流程
1. 开发过程中持续在此文件顶部添加新记录
2. 版本发布前整理和分类内容
3. 复制内容到 CHANGELOG.md，更新版本号和日期
4. 清空此文件（保留说明部分），准备下一轮开发

---

---

### ObjectPool
- 修复 非池化回退分支二次 FinishSpawning 触发引擎断言
- 修复 公开无效引用清理缺少内部加锁
- 修复 预测预分配超过目标数量
- 修复 无效分帧配置导致 Ticker 空转
- 修复 预分配达到池硬上限后持续重试
- 修复 立即预分配结束时未统一清理状态和记录结束时间
- 测试 延迟回退生成仅完成一次的确定性回归护栏
- 测试 增加公开清理接口护栏
- 测试 增加预测目标上限护栏
- 测试 增加预分配配置验证护栏
- 测试 增加预分配容量耗尽生命周期护栏
- 调整 默认值测试改用瞬态实例避免 ini 干扰
- 调整 延迟回退追踪集补契约说明注释

### ObjectPoolEditor
- 修复 生成节点缺少类时仅产生编译警告
- 修复 生成节点展开时未完整校验引脚与运行时函数签名
- 测试 增加对象池生成节点真实展开编译护栏

### XTools_EnhancedCodeFlow
- 新增 旧子系统开关启动时自动迁移到项目 DefaultGame.ini，写入成功后再清理旧键
- 优化 遗留日志类别收敛至 LogECF 统一管控
- 修复 计时器节点用户 Stop 后同帧双播完成回调

### X_AssetEditor
- 调整 废弃开关补充界面级迁移指引
- 修复 导航测试缺少 fixture 地图时错误报告为通过

### SplineMovement
- 修复 AI 寻路失败或中止后永久停滞无自愈
- 优化 重寻路判距统一水平面口径免开方

### SplineMovementEditor
- 优化 节点动作生成器创建失败时安全跳过注册
- 测试 增加开始与中断双事件真实展开编译护栏

### FormationSystem
- 修复 全无效单位过渡破坏进行中状态致悬挂
- 优化 制动带双阈值迟滞消除走走停停蠕行

### XToolsCore
- 修复 非编辑器构建忽略显式 Info 屏显请求
- 修复 统一日志遗漏结点轨道类别
- 测试 增加日志清单覆盖护栏

### BlueprintExtensions
- 修复 循环节点展开失败分支未断链残留半展开态

### PointSampling
- 修复 泊松裁剪超上限改为截断保住数量契约

### Sort
- 修复 字符串去重错误地将默认忽略大小写比较用于区分大小写模式
- 修复 向量容差去重漏检非相邻项

### SortEditor
- 修复 智能排序重建丢失动态连接
- 优化 默认值设置日志级别
- 测试 增加向量与结构体智能排序真实展开编译护栏
- 测试 隔离临时蓝图资产注册表预期告警

### XTools
- 修复 点采样拒绝非有限及越界参数
- 修复 小尺寸网格采样半径区间反转
- 测试 增加点采样输入验证护栏

### XTools_SwitchLanguage
- 修复 语言与地区一致时同步切换
- 修复 英语区域识别错误
- 测试 增加语言路由护栏

### 构建
- 调整 全量源文件补齐 UTF-8 BOM 根治中文注释乱码隐患
