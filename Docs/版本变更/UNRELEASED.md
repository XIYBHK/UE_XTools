# 待发布更新 (UNRELEASED)

> 此文档记录尚未发布的功能更新、修复和移除，用于下次版本发布时的描述参考。
> 发布新版本时，将此文件内容合并到 CHANGELOG.md，然后清空此文件（保留说明部分）。

---

### XTools
- **调整** 贝塞尔曲线点拆分为纯计算与显式调试绘制节点
- **修复** 默认速度模式未应用进度映射曲线的问题
- **新增** 贝塞尔导弹轨迹、控制点生成和标架状态重置节点
- **优化** 贝塞尔导弹噪声按运动进度平滑衰减，匀速映射不再改变端点包络
- **调整** 贝塞尔导弹节点提示，并将经过时间引脚明确为噪声采样时间
- **修复** 贝塞尔导弹输出切线和旋转跟随叠加噪声后的实际移动方向
- **修复** 带延迟循环节点在事件图编译阶段被误判为函数图的问题
- **修复** 六种循环增强节点触发 Break 后可能重复执行 Completed 的问题
- **修复** 五种带延迟循环节点重入时覆盖共享索引并丢失后续执行的问题，运行中的重复调用现在会被忽略
- **修复** 带延迟的 WhileLoop 在 Delay 为 0 时仍进入 Latent 流程的问题，五种延迟循环现在统一为零延迟同步执行
- **调整** 带延迟循环节点明确为按间隔启动循环体，不等待循环体内的 Latent 或异步操作

### XToolsCore
- **优化** 编辑器消息面板仅接收警告和错误

### ObjectPool
- **修复** 补全非反射池数据中的类引用收集
- **优化** 激活和重置路径直接遍历组件集合

### FormationSystem
- **修复** 分离权重为负数时安全返回零向量

### BlueprintExtensions
- **修复** Map Append 展开节点统一校验内部连线

### XTools_BlueprintAssist
- **调整** 默认全局禁用自动格式化

### PointSampling
- **新增** 矩形网格独立横纵间距节点
- **优化** 泊松活跃点移除改为常数复杂度

## 日志格式说明

### 模块分类
按模块名称分为独立章节，每个章节内使用简洁的列表格式记录变更

### 记录格式
```markdown
### 模块名
- 类型 描述
- 类型 描述
```

### 类型说明（单字或双字）:
- **新增** / 新增功能
- **优化** / 改进现有功能
- **修复** / 修复问题
- **移除** / 删除功能
- **调整** / 参数或行为调整
- **集成** / 新增模块集成
- **本地化** / 本地化工作

### 内容要求
1. **简洁**：每行记录一个独立变更，描述控制在 20 字内
2. **清晰**：使用"动词 + 对象 + （效果）"格式
3. **关键信息**：包含重要的参数变化、性能数据、修复的问题
4. **不要**：避免详细技术细节、文件路径、长篇问题分析

### 发布流程
1. 开发过程中持续在此文件顶部添加新记录
2. 版本发布前整理和分类内容
3. 复制内容到 CHANGELOG.md，更新版本号和日期
4. 清空此文件（保留说明部分），准备下一轮开发

---
